<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.xlogistx</groupId>
        <artifactId>xlogistx-mvn</artifactId>
        <version>LATEST</version>
    </parent>

    <name>xlogistx-iot</name>
    <artifactId>xlogistx-iot</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <properties>
    	<pi.current.version>1.2-SNAPSHOT</pi.current.version>
    </properties>

    <build>
        <directory>target</directory>
        <resources>
            <resource>
                <directory>src/main/java/</directory>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven-plugin.version}</version>
                <configuration>
                    <source>${jdk.version}</source>
                    <target>${jdk.version}</target>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>${maven-plugin-source.version}</version>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <version>${maven-javadoc.version}</version>
                <configuration>
                    <failOnError>false</failOnError>
                </configuration>
                <executions>
                    <execution>
                        <id>attach-javadocs</id>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <dependencies>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
        </dependency>

       

        <dependency>
            <groupId>org.zoxweb</groupId>
            <artifactId>zoxweb-core</artifactId>
            <scope>compile</scope>
        </dependency>
        
        <dependency>
            <groupId>org.zoxweb</groupId>
            <artifactId>zoxweb-shiro</artifactId>
            <scope>compile</scope>
        </dependency>

        

        <dependency>
            <groupId>javax.servlet</groupId>
            <artifactId>javax.servlet-api</artifactId>
        </dependency>

        <dependency>
            <groupId>commons-fileupload</groupId>
            <artifactId>commons-fileupload</artifactId>
             <scope>compile</scope>
        </dependency>

        <dependency>
            <groupId>com.google.code.gson</groupId>
            <artifactId>gson</artifactId>
            <scope>compile</scope>
        </dependency>

        <dependency>
            <groupId>javax.websocket</groupId>
            <artifactId>javax.websocket-api</artifactId>
        </dependency>

        <dependency>
            <groupId>javax.websocket</groupId>
            <artifactId>javax.websocket-client-api</artifactId>
        </dependency>

        <dependency>
            <groupId>org.glassfish.tyrus</groupId>
            <artifactId>tyrus-websocket-core</artifactId>
        </dependency>

        <dependency>
            <groupId>org.glassfish.tyrus.bundles</groupId>
            <artifactId>tyrus-standalone-client-jdk</artifactId>
        </dependency>

        <dependency>
            <groupId>org.apache.shiro</groupId>
            <artifactId>shiro-core</artifactId>
            <scope>compile</scope>
        </dependency>

        <dependency>
            <groupId>org.apache.shiro</groupId>
            <artifactId>shiro-web</artifactId>
            <scope>compile</scope>
        </dependency>
        
      	
        <dependency>
		  <groupId>javax.cache</groupId>
		  <artifactId>cache-api</artifactId>
		  <scope>runtime</scope>
		</dependency>
		<dependency>
	      <groupId>org.ehcache</groupId>
	      <artifactId>ehcache</artifactId>
	      <scope>runtime</scope>
	    </dependency>
	   
        <dependency>
            <groupId>org.jsoup</groupId>
            <artifactId>jsoup</artifactId>
            <scope>compile</scope>
        </dependency>

        <dependency>
            <groupId>javax.mail</groupId>
            <artifactId>javax.mail-api</artifactId>
            <scope>compile</scope>
        </dependency>
        <!--
        <dependency>
        	<groupId>com.pi4j</groupId>
        	<artifactId></artifactId>
        	<version>${pi.current.version}</version>
        	<systemPath>${project.basedir}/src/main/resources/libs/-${pi.current.version}</systemPath>
        </dependency>
		-->
		
		<dependency>
        	<groupId>com.pi4j</groupId>
        	<artifactId>pi4j-core</artifactId>
        	<version>${pi.current.version}</version>
        	<scope>provided</scope>
        	<!--  <systemPath>${project.basedir}/src/main/resources/lib/pi4j-core-${pi.current.version}.jar</systemPath> -->
        </dependency>
        <dependency>
        	<groupId>com.pi4j</groupId>
        	<artifactId>pi4j-device</artifactId>
        	<version>${pi.current.version}</version>
        	<scope>provided</scope>
        	<!-- <systemPath>${project.basedir}/src/main/resources/lib/pi4j-device-${pi.current.version}.jar</systemPath> -->
        </dependency>
        <dependency>
        	<groupId>com.pi4j</groupId>
        	<artifactId>pi4j-gpio-extension</artifactId>
        	<version>${pi.current.version}</version>
        	<scope>provided</scope>
        	<!--  <systemPath>${project.basedir}/src/main/resources/lib/pi4j-gpio-extension-${pi.current.version}.jar</systemPath> -->
        </dependency>
       

    </dependencies>

</project>
